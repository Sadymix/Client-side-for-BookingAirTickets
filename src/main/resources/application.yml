app:
  user:
    name: admin
    password: admin
    grantType: password
  token:
    url: http://localhost:8080/oauth/token
  get:
    single:
      user:
        url: http://localhost:8080/api/users
  client:
    clientId: ClientId
    secret: secret
logging:
  level:
    org:
      springframework:
        web:
          client:
            RestTemplate: DEBUG
spring:
  profiles:
    active: smoke-test
  main:
    web-application-type: none